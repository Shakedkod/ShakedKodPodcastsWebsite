import React from "react";

import { Navbar, Header } from "../components";

const Home = () => {
    return (
        <div className="bg-black">
            <Navbar/>
            <Header/>
        </div>
    );
};

export default Home;